import { SlideItemProp } from "@/app/includes/types"

export const SlideItem = ({
    id: _id,
    description: _description,
    graphics: _graphics,
    title: _title
}:SlideItemProp)=>{
    return <div >

    </div>
}